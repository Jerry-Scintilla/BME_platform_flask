# gunicorn.conf

bind = "127.0.0.1:5000"  # 2026-09-17：收回本机回环——流量一律过 nginx（ProxyFix 信任的唯下一跳）；直连 5000 可伪造 XFF 绕限流
workers = 4
backlog = 2048
pidfile = "log/gunicorn.pid"
accesslog = "log/access.log"
errorlog = "log/debug.log"
timeout = 600
debug=False
capture_output = True
daemon = False