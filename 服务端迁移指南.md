# 服务端迁移更新指南

> 供服务端维护 / Agent 执行迁移更新。**本次升级（20260710）**：2025-06-12 基线 → 座位看板 + 营期管理系统 + RBAC + notification 连接。
> 生产库为**老库原地升级**，保留真实用户数据，不重建。

---

## ⚠️ 升级前必做
1. **备份数据库**（回滚底气，跳过等于裸奔）：
   ```bash
   mysqldump -u root -p sysu_bme > backup_$(date +%Y%m%d_%H%M).sql
   ```
2. 确认 `.env` 里 `JWT_SECRET` 已换成随机强密钥（旧值是占位符 `your-secret-key-change-in-production`）。
3. 确认 `DB_PORT` 是生产 MySQL 端口（非本地 3300）、`REDIS_URL` 指向生产 Redis。

---

## 执行步骤（按序）

### 1. 拉最新代码（后端 master）
```bash
cd /opt/BME_platform_flask
git fetch origin
git checkout master
git pull origin master
```

### 2. 更新依赖（本周新增 APScheduler / tzlocal）
```bash
source .venv/bin/activate          # 若无：python3.12 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

### 3. 数据库迁移（按序，全部幂等，可重复运行）
```bash
cd /opt/BME_platform_flask
python scripts/migrate/migrate_01_user_role.py          # user 加 role 列 + admin→super_admin 回填
python scripts/migrate/migrate_02_camp_schema.py        # 建 6 营期表 + 4 老表加 6 列
python scripts/migrate/migrate_03_feature_camp.py       # camp_session.is_featured + camp_join_request 表
python scripts/migrate/migrate_04_join_selected_days.py # camp_join_request.selected_days
python scripts/migrate/migrate_05_perms.py              # 17 下划线权限 + super_admin 授权
python init_seats.py                                    # 建 study_room/seat 表 + 106 房 40 座位
```
> 顺序依赖：`migrate_05` 依赖 `migrate_01`（按 role='super_admin' 授权）；`03/04` 依赖 `02` 建表。详见 `scripts/migrate/README.md`。

### 4. 前端构建（两个前端都要改 `.env.production` 再 build）
两个前端的 `.env.production` 里 `VITE_API_BASE_URL` 当前是旧内网地址 `172.25.56.83:8080/api`，**build 前改成生产域名**（如 `https://ameii.sysu.edu.cn/api`）。

```bash
# 用户前端（base /AMEII/）
cd /opt/bme-frontend
vim .env.production            # 改 VITE_API_BASE_URL
npm install && npm run build
cp -r dist/* /var/www/bme-frontend/

# 管理前端（base /admin/）
cd /opt/bme-admin
vim .env.production            # 同上
npm install && npm run build
cp -r dist/* /var/www/bme-admin/
```

### 5. 重启后端 + nginx
```bash
# 按现有生产方式（二选一，与 nginx proxy_pass / 前端 baseURL 对齐）：
systemctl restart bme          # 若有 systemd unit
# 或手动：kill 旧进程后
#   gunicorn -c gunicorn.conf app:app      # 绑 5000
#   nohup .venv/bin/python app.py &        # 绑 5001
nginx -t && nginx -s reload    # 若前端路径/base 有变更
```

---

## 验证
- **后端**：启动日志无 Traceback；登录接口返回含 `role` 与 `permissions`。
- **DB**：`user.role` 列存在；`user_course / medal_user / check_record / notification` 的 camp_session_id 等列齐全；`camp_*` 7 张表 + `study_room`/`seat` 存在；`permission` 表 17 个下划线权限；`seat` 表 40 座位。
- **前端**：浏览器 `/AMEII/` 与 `/admin/` 正常载入；Network 面板看 API 请求打到正确后端。
- **业务**：座位看板打卡实时显示；营期列表/成员/选课/考勤看板通畅。

---

## 回滚
- **代码**：`git reset --hard <上一个生产 commit>` + 重启。
- **数据库**：用步骤 0 的 `mysqldump` 备份恢复。
- 加列均 nullable / 有默认值，回滚用 `ALTER TABLE ... DROP COLUMN ...`，不伤老数据；新表回滚 `DROP TABLE ...`。

---

## ⚠️ 注意事项
- **`seed.py` 不要整体跑**（会灌 4 个测试账号 + 演示课程 + 测试通知）。权限已由 `migrate_05_perms.py` 处理。
- **`data/` 目录被 gitignore**（头像 / 课程封面 / 作业上传）：服务器需手动建 `data/{avatars,article,course,cover,error_images,homeCover,homework}` 并 chown 给运行用户，否则上传失败。
- **端口不一致**：`gunicorn.conf` 绑 5000、`app.py` 绑 5001 —— 定一个，nginx `proxy_pass` 与前端 baseURL 对齐。
- **`exts.py` limiter Redis 写死 `localhost`**：若生产 Redis 非本机，需改成读 `REDIS_URL`（否则限流连不上）。
- 本次迁移脚本（`scripts/migrate/`）幂等，重跑安全。
